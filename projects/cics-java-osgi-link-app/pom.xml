<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ibm.cicsdev</groupId>
        <artifactId>cics-java-osgi-link</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>cics-java-osgi-link-app</artifactId>
    <packaging>jar</packaging>
    <name>CICS OSGi link application - Application</name>

    <dependencies>
        <dependency>
            <groupId>com.ibm.cics</groupId>
            <artifactId>com.ibm.cics.server</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ibm.cics</groupId>
            <artifactId>com.ibm.cics.jcicsx</artifactId>
        </dependency>

        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>osgi.annotation</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>biz.aQute.bnd</groupId>
                <artifactId>bnd-maven-plugin</artifactId>
                <configuration>
                    <!-- Note: The @CICSProgram proxies contain EJB annotations for Enterprise Java
                    programs. This statement marks the EJB package as optional for OSGi resolution. -->
                    <bnd>Import-Package: javax.ejb;resolution:=optional, *</bnd>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>